apiVersion: v1
kind: Service
metadata:
  name: bss-partner-management-svc
spec:
  selector: { app: bss-partner-management-services }
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
